# Base URL of pmis_backend API (e.g., http://localhost:8000 or the deployed URL)
REACT_APP_API_BASE_URL=http://localhost:8000

# Public site URL for redirects (if needed in future features)
REACT_APP_SITE_URL=http://localhost:3000

# Default language code (en, hi, ta, te, bn, mr, kn, ml, gu, pa etc.)
REACT_APP_DEFAULT_LANG=en
